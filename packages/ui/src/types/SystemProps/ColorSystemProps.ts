import { ColorType } from '../designTokens';

export interface ColorSystemProps {
  /**
   * background-color
   */
  bg?: ColorType;
  /**
   * color
   */
  color?: ColorType;
  /**
   * border-color
   */
  borderColor?: ColorType;
}
