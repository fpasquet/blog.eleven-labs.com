import { ColorSystemProps } from '../../types/SystemProps';

export const colorSystemProps: Record<keyof ColorSystemProps, string[]> = {
  bg: ['background-color'],
  color: ['color'],
  borderColor: ['border-color']
};
